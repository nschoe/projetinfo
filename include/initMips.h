#ifndef _INITMIPS_H_
#define _INITMIPS_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "mips.h"

int initMips( mips * puP, const char * name, const char * dicoFile );

#endif
